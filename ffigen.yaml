# Run with `flutter pub run ffigen --config ffigen.yaml`.
name: UsbEscPrinterWindowsBindings
description: |
  Bindings for `src/usb_esc_printer_windows.h`.

  Regenerate bindings with `flutter pub run ffigen --config ffigen.yaml`.
output: 'lib/usb_esc_printer_windows_bindings_generated.dart'
headers:
  entry-points:
    - 'src/usb_esc_printer_windows.h'
  include-directives:
    - 'src/usb_esc_printer_windows.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
